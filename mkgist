#!/usr/bin/env python3

import requests
import sys

ENDPOINT = "https://api.github.com/gists"

# curl -X POST -d @test.json https://api.github.com/gists
r = requests.post('http://httpbin.org/post', data = {'key':'value'})

if __name__ == "__main__":
    if len(sys.argv) <= 1:
        print("Usage: mkgist file")
        sys.exit(1)

    print(sys.argv)


# {
#     "description": "the description for this gist",
#     "public": true,
#     "files": {
#         "file1.txt": {
#               "content": "String file contents"
#         }
#     }
# }
